# Optional. For production (Vercel etc.), set to your API origin — no path, no trailing slash.
# Example: https://ironlog-api.onrender.com
# Leave unset locally: Vite proxies /api to the Express server.
# VITE_API_URL=
